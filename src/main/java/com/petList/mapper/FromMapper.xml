<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--FromMapper-->
<mapper namespace="com.petList.mapper.FromMapper">
<!--搜尋貓-->
    <select id="CatFrom" parameterType="CatFrom">
        select * from petlist.catfrom
    </select>
<!--搜尋狗-->
    <select id="DogFrom" parameterType="DogFrom">
        select * from petlist.dogfrom
    </select>
</mapper>
